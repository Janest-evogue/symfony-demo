{# http/session.html.twig #}
{% extends 'base.html.twig' %}

{% block body %}
    {#
    app.session pour accéder à la session
    depuis un template twig
    #}
    {{ dump(app.session.all) }}
{% endblock %}
